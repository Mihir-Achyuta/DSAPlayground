const prompts = require("prompts");

const { errorHandler } = require("../../../handlers/error/errorHandler");

function binarySearchTreeCli() {
  try {
  } catch (error) {
    errorHandler(error);
  }
}

module.exports = { binarySearchTreeCli };
